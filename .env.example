# Databricks workspace hostname — no https:// prefix
# e.g. myworkspace.cloud.databricks.com
DATABRICKS_HOST=

# Databricks service principal OAuth app client ID (federation mode)
DATABRICKS_SP_CLIENT_ID=

# Unity Catalog coordinates — must match tables created via the setup SQL
DATABRICKS_UC_CATALOG=
DATABRICKS_UC_SCHEMA=
DATABRICKS_UC_TABLE_PREFIX=
